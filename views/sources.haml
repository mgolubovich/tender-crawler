- @title = 'Sources list'
%h1 Sources list
.container-fluid
  %h4
    %a{ href: "/sources/new"} Add new source
.container-fluid
  %table.table.table-condensed.table-bordered
    %thead
      %tr
        %td Name
        %td Tenders count
        %td Selectors count
        %td Update frequency
        %td Edit
        %td Delete
        %td Activity?
    %tbody
      - @sources.each do |source|
        %tr{:class => source.is_active ? "success" : "default" }
          - if source.url.empty?
            %td= source.name
          - else
            %td
              %a{ href: "#{source.url}" }
                %b= source.name
          %td
            %a{ href: "/tenders?source_id=#{source.id}"}= source.tenders.count
          %td= source.selectors.count
          %td= source.tenders_update_frequency
          %td
            %a{ href: "/sources/edit/#{source.id}"} Edit
          %td
            %a{ href: "/sources/destroy/#{source.id}"} Delete
          %td= source.is_active ? "Yes" : "No"
  = will_paginate @sources, renderer: BootstrapPagination::Sinatra
